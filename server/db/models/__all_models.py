from server.db.models import users, groups, user_groups, expenses, payments, debts

User = users.User
Group = groups.Group
UserGroup = user_groups.user_groups

Expense = expenses.Expense
Payment = payments.Payment
Debt = debts.Debt
